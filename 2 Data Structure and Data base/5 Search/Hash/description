A. Hash
Time limit: 1000 ms

Memory limit: 256 MB

Standard I/O

要求：

采用顺序表存储元素，元素类型为无符号 int。保证元素不为 0，也不为 0xffffffff。
Hash 表的容量为 2 ^ 12，初始状态表为空。
Hash 函数采用平方取中法，无符号 int 为 32 位整型数，平方后为64位，取中间 12 位。
处理冲突时采用开放定址法。增量取二次散列函数 h(x)。
h(x) = x mod (2 ^ 12) 当 x 为奇数时

h(x) = (x + 1) mod (2 ^ 12) 当 x 为偶数时 

Input Description
输入数据有 10 ^ 4 行，每行有两个整数 a，b。

a 为一个无符号 int 型数，代表要操作的元素；b 为 1 或者 0。

当 b 为 1 时，将元素 a 插入 hash 表。

当 b 为 0 时，查询 a 元素是否在 hash 表中，若存在则删除它。

Output Description
输出有 10 ^ 4 + 2 行。

对于前 10 ^ 4 行，如果此次操作是插入，输出尝试次数，及插入的位置；如果此次操作是查找，输出尝试次数，及元素所在的槽（如果不在表中输出 -1）。

最后两行输出查找成功及失败时的平均尝试次数，用分数输出，注意不用约分。

Hint
虽然题很简单...但是知识点要会啊...
