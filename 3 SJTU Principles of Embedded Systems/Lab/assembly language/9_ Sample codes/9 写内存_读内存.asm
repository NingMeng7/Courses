DAT	SEGMENT	AT	4000H
	DATA	DB	49152	DUP(?)
DAT	ENDS

STACK	SEGMENT
		ST	DB	100 DUP(?)
STACK	ENDS

CODE	SEGMENT
		ASSUME	CS:CODE, DS:DAT, SS:STACK, ES:DAT
		
MAIN	PROC	FAR
START:	MOV	AX,	STACK
		MOV	SS, AX
		PUSH DS
		MOV AX, 0
		PUSH AX
		
		MOV AX, DAT
		MOV DS, AX
		MOV ES, AX
		MOV DI, 0
		MOV CX, 49152
		MOV AL, 55H
		CLD	;	DF<-0	
		REP	STOSB
		MOV CX, 49152
		MOV SI, 0
AGAIN:	LODSB
		CMP AL, 55H
		JNZ	ERR1
		LOOP	AGAIN
		MOV	AL, 7EH
		JMP	EXT
ERR1:	MOV AL, 81H
EXT:	NOP
		RET
MAIN	ENDP
CODE	ENDS
		END	START
