DATA SEGMENT 
X DW ?
Y DW ?
BIG DW ?
DATA ENDS
CODE SEGMENT
ASSUME CS:DATA, DS:DATA
MAIN: MOV AX, DATA
      MOV DS, AX
      
START:
      MOV AX, X
      MOV BX, Y
      CMP AX, BX
      JG LOP
      XCHG AX, BX
  LOP:MOV BIG, AX
      MOV AX, 4C00H
      INT 21H
CODE ENDS
END MAIN
