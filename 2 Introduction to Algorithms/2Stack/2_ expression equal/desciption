A. equal
Time limit: 1000 ms

Memory limit: 256 MB

Standard I/O

这次的任务是从大量奇怪的代数式中挑出与给定代数式等价的式子。

完成任务的人将会获得第五次上机分数作为奖励。

如果邪恶的助教不继续加测试集的话，目前的评分标准是：

如果提交并且通过至少一个测试点，获得60分基础分。
No.1 - No.10 测试数据：每通过一个点获得3分。
No.11 - No.15 测试数据：每通过一个点获得2分。
代数式有如下性质

只会出现一个变量x，常量都是小于32767的正整数。
式子中含有四种运算：加法、减法、乘法与乘方，没有除法（为了避免奇怪的除零错）。优先级按照正常运算优先级计算。可能有大量括号。式子中可能会出现一些空格，但空格不会出现在一个常量内部。
幂指数不超过10，而且不会出现零次幂的情况。

Input Description
第一行是一个整数n，满足2 <= n <= 26。

第二行是给定的代数式，保证表达式合法。

第三行至第n+2行共n行，每行有一个代数式，它们的编号分别为A，B，C....

Output Description
只有一行，包括一个仅由升序大写字母组成的字符串，表示哪些式子与所给代数式等价。

Sample
INPUT 

3
( x + 1 ) ^2
(x-1)^2+4*x
x + 1+ x
x^2 + 2 * x * 1 + 1^2 + 10 -10 +x -x
OUTPUT 

AC
Hint
对于30%的数据，代数式中只有'+'与'-'。
对于所有数据，没有除法，均可能出现括号；保证至少有一个式子与所给式子等价；保证变量不会出现在幂指数上；代数式字符串在去除括号及空格后，保证严格按照 
  常量/变量 -> 运算符 -> 常量/变量 -> 运算符 -> ...... -> 运算符 -> 常量/变量 的顺序出现。代数式字串长度不会超过255。
